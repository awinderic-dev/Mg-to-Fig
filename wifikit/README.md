# WiFiKit 设备自检界面

这是一个轻量的移动端界面原型，可直接在浏览器中预览。

## 文件

- `index.html`：页面结构
- `styles.css`：视觉样式、手机外框、检测状态和响应式适配

## 预览

在本目录启动静态服务后访问：

```bash
python3 -m http.server 4173 --bind 127.0.0.1
```

然后打开：

```text
http://127.0.0.1:4173/
```
